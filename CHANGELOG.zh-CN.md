# 更新日志

[English](CHANGELOG.md) | **简体中文**

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的基本格式。

## [Unreleased]

## [1.3.1] - 2026-08-05

### Fixed

- 修复所见即所得编辑器扫描“空有序列表项 + 换行”时重复停留在同一行、导致主线程卡死的问题。
- 在空有序列表标记后按退格可稳定退出到普通段落；之后继续输入或按回车不会重新进入列表。

## [1.3.0] - 2026-08-02

### Added

- 所见即所得编辑器粘贴疑似 Markdown 时提供“转换为 Markdown / 粘贴源码 / 取消”三种选择。
- 新增加粗 `⌘B` 与删除线 `⇧⌘X` 快捷键，源码和所见即所得模式均可使用。
- “检查更新”会连接 GitHub Releases 比较稳定版本，展示更新说明和下载进度，并准备可原位替换的 macOS 应用包。
- 新增独立更新助手，支持退出后替换、启动验证、失败回滚、管理员授权与临时文件清理。
- 新增英文和简体中文界面；全新安装默认英文，用户选择会在本机持久化。

### Changed

- 内容区左右安全距离小幅增加，标签页、内容容器、按钮和悬浮目录统一为 6 pt 小圆角。
- 移除窗口内“打开、保存、复制、图片”文字按钮；标签页与其余图标控件合并到同一条紧凑顶部栏。
- 悬浮标题目录进一步收紧尺寸、行距和视觉层级，减少对正文的遮挡。
- 在线更新不再打开 DMG 让用户重复拖拽安装；确认后直接替换当前启动的应用路径。
- GitHub 面向用户的文档默认英文，并提供完整中文版本链接；以后 issue 使用英文标题和英文优先正文。

### Fixed

- 表格段落终止符保留原生表格布局属性，避免重排、缩放或编辑后相邻边框偶发断开。
- 表格滚动时按完整行列几何重绘连续像素网格，修复局部重绘导致的横线、竖线分段。
- 连续表格覆盖层直接使用 AppKit 原生边框区域，避免首尾行内部出现重复横线，同时保留表格外部间距。
- 标签切换恢复“顶部可见字符锚点 + 行内像素偏移”，避免光标正确但正文视野整体上移。
- 标题栏字标改用随系统标签色变化的模板渲染，深色模式下保持清晰。
- 段落中的单个软换行不再在重新解析后被折叠为空格。

## [1.2.1] - 2026-07-11

### Changed

- 悬浮标题目录移到文本区域右上角，并使用更轻、更透明的背景。
- 应用每次启动都默认进入即时渲染模式，不再恢复为源码模式。

## [1.2.0] - 2026-07-11

### Added

- 文本区域左上角悬浮 H1–H6 标题目录，支持点击跳转与收起。
- `/` 菜单扩展到 1–6 级标题、常用行内格式、任务列表和表格，并改为双列布局。
- 未保存文档支持直接插图，首次保存时自动迁移到相对附件目录。
- 支持从访达拖放图片，以及粘贴截图或已复制的图片文件。
- 原生查找替换、`⌘P` 最近文件快速打开、HTML/PDF 导出。
- 标签页分别保存源码与渲染模式的光标和滚动位置。
- 启动时间、内存、二进制和 App 包体积性能预算检查。

### Changed

- 标签页切换复用编辑器实例并移除过渡动画，降低切换延迟感。
- README 明确项目由 AI 完成编码。
- CI 增加性能预算门禁，所有新增能力继续保持零新增第三方依赖。

## [1.1.0] - 2026-07-11

### Added

- 行首 `/` Markdown 格式菜单，支持键盘和鼠标操作。
- 退格关闭 `/` 菜单并保留普通斜杠。
- 本地图片插入、附件目录管理和渲染编辑器内直接显示。
- 开源许可证、贡献指南、安全政策、CI 与敏感信息审计脚本。

### Changed

- 会话快照改为合并写入，降低连续输入时的磁盘 I/O。
- 编辑样式刷新缩小到受影响行。
- 图片缓存设置数量和内存上限。
- 清理“关于”窗口中的个人化文案。
